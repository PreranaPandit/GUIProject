from tkinter import *

'''
root = Tk()

#creating a label widget
myLabel1 = Label(root, text="Tkinter Program Beginning")
myLabel2 = Label(root, text="I am Prerana Pandit")

#Shoving it onto the sreen based upon x-axis and y-axis that does not move having a constant position
myLabel1.grid(row=0, column=0)
myLabel2.grid(row=1, column=0)

'''
# the changes of column of label2 does not changes as there are not any texts to assign
'''

root.mainloop()
'''


# # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # #


'''
root = Tk()

#creating a label widget
myLabel1 = Label(root, text="Tkinter Program Beginning")
myLabel2 = Label(root, text="I am Prerana Pandit")
myLabel3 = Label(root, text="jhghfgdfgfd+empty")

#Shoving it onto the sreen based upon x-axis and y-axis that does not move having a constant position
myLabel1.grid(row=0, column=0)
myLabel2.grid(row=1, column=5)
myLabel3.grid(row=1, column=1)
'''
# the changes of column of label2 does not changes as there are not any texts to assign
'''

root.mainloop()
'''



root = Tk()

#creating a label widget
myLabel1 = Label(root, text="Tkinter Program Beginning").grid(row=0, column=0)
myLabel2 = Label(root, text="I am Prerana Pandit").grid(row=1, column=5)
myLabel3 = Label(root, text="jhghfgdfgfd+empty").grid(row=1, column=1)

#Shoving it onto the sreen based upon x-axis and y-axis that does not move having a constant position
# myLabel1.grid(row=0, column=0)
# myLabel2.grid(row=1, column=5)
# myLabel3.grid(row=1, column=1)
'''
# the changes of column of label2 does not changes as there are not any texts to assign
'''
root.mainloop()

